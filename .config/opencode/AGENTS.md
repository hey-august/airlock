# User Setup

- Git uses SSH with unsaved/loaded keys (ssh-agent) for authentication
- Git operations other than cloning public HTTP repos will prompt for a password
- Do not attempt non-interactive git operations that require authentication — they will hang or fail
